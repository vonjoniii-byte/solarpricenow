// Enums for calculation engine — §4.2
// Pure Dart — NO Flutter imports.

enum SetupType {
  nothing,
  panelsOnly,
  panelsAndBattery,
}

enum UsagePattern {
  mostlyDay,
  evenSplit,
  mostlyNight,
}
